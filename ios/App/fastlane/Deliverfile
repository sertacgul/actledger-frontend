# Deliverfile - App Store submission configuration

# App metadata
app_identifier("com.ataolaitech.actledger")
username(ENV["APPLE_ID"] || "changeme@example.com")

# Metadata path
metadata_path("./fastlane/metadata")
screenshots_path("./fastlane/screenshots")

# Submission settings
submit_for_review(false)
automatic_release(false)
force(true)

# Skip uploading screenshots if folder is empty
skip_screenshots(false)

# Primary category
primary_category("MZGenre.Business")

# Copyright
copyright("2026 ATAOL AI Techs")

# Price and availability are set manually in App Store Connect
# Age rating is set manually in App Store Connect
